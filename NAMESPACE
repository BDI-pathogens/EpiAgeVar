# constants exposed by package

# functions exposed by package

# required packages
import( rstan )
import( Rcpp )
import( rstantools )
import( StanHeaders )
import( data.table )
import( plotly)
import( R6 )